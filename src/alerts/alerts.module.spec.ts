import { AlertsModule } from './alerts.module';

describe('AlertsModule', () => {
  it('should be defined', () => {
    const module = new AlertsModule();
    expect(module).toBeDefined();
  });
});


